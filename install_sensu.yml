---
all:
  vars:
    ansible_ssh_common_args: >
      -o IdentitiesOnly=yes
      -o BatchMode=yes
      -o UserKnownHostsFile=/dev/null
      -o StrictHostKeyChecking=no
      -i demo
  children:
    backends:
      hosts:
        dm1.heytate.com:
          ansible_host: amq1.heytate.com
          ansible_user: ctate
    agents:
      hosts:
        dm1.heytate.com:
          ansible_host: dm1.heytate.com
          ansible_user: ctate
        ansible1.heytate.com:
          ansible_host: ansible1.heytate.com
          ansible_user: ctate
